{
  "name": "stream-analyzer-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "cd server && node index.js",
    "build": "vite build",
    "preview": "vite preview",
    "db:migrate": "cd server && node db/migrate.js",
    "db:seed": "cd server && node db/seed.js",
    "setup": "cd server && npm install && node db/migrate.js && node db/seed.js"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {}
}

